// Deprecated legacy Three.js entrypoint intentionally replaced by src/main.tsx + src/game/engine.ts.
// Kept as a zero-logic marker during migration so legacy tooling never bootstraps a second renderer.
export {};
